# def double(x):
#     return x*2
double = lambda x: x*2
cube = lambda x: x*x*x
avg = lambda x,y: (x+y)/2 
print(avg(60, 32))
print(double(5))
print(cube(69))


# instead 0f using def function , its alot better to use lambda as its much easier to use